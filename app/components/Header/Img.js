import styled from 'styled-components';

import NormalImg from 'components/Img';

const Img = styled(NormalImg)`
  width: 50%;
  margin: 0 auto;
  display: block;
  padding: 20px;
`;

export default Img;
